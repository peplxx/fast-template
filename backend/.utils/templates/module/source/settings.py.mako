# ${module_name.title()}
class ${module_name.title()}Settings:
    # ${module_name.title()} module settings
    # Used to state specific module variables
    # VARIABLE: str = environ.get("VARIABLE", "DEFAULT")
    pass
